---
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayPolicy
metadata:
  name: grafana-mcp-authentik-authn
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: grafana-mcp
  traffic:
    jwtAuthentication:
      mode: Strict
      providers:
        - issuer: https://sso.${SECRET_DOMAIN}/application/o/grafana-mcp/
          audiences:
            - grafana-mcp
          jwks:
            remote:
              backendRef:
                name: authentik-server
                namespace: auth
                port: 80
              jwksPath: /application/o/grafana-mcp/jwks/
      mcp:
        provider: Authentik
        clientId: grafana-mcp
        resourceMetadata:
          resource: https://grafana.mcp.${SECRET_DOMAIN}
          scopesSupported:
            - openid
            - profile
          bearerMethodsSupported:
            - header
    authorization:
      action: Allow
      policy:
        matchExpressions:
          - 'has(jwt.groups) && jwt.groups.exists(g, g == "Hoohoot Developers")'

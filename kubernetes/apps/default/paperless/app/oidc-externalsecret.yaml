---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: paperless-oidc
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: hashicorp-vault
  target:
    name: paperless-oidc
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        client-id: "{{ .PAPERLESS_CLIENT_ID }}"
        client-secret: "{{ .PAPERLESS_CLIENT_SECRET }}"
  dataFrom:
    - extract:
        key: default/paperless

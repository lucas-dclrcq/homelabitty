---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: robovac
spec:
  hostnames:
    - robovac.${LIMITED_DOMAIN}
  parentRefs:
    - name: internal
      namespace: network

  rules:
    - backendRefs:
        - name: robovac
          port: 80
      matches:
        - path:
            type: PathPrefix
            value: /
---
apiVersion: v1
kind: Service
metadata:
  name: robovac
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80
      name: admin
---
apiVersion: discovery.k8s.io/v1
kind: EndpointSlice
metadata:
  name: robovac
  labels:
    kubernetes.io/service-name: robovac
addressType: FQDN
ports:
  - name: admin
    protocol: TCP
    port: 80
endpoints:
  - addresses:
      - "robovac.${INTERNAL_DOMAIN}"

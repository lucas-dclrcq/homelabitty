---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-16-vector-pgbackrest-repo
  labels:
    app.kubernetes.io/component: postgres-16-vector-pgbackrest-repo
spec:
  capacity:
    storage: 50Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  nfs:
    server: "truenas.${INTERNAL_DOMAIN}"
    path: /mnt/tank/Backup/postgresql/vector

---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: renovate
  namespace: infrastructure
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: 1.5.1
      interval: 30m
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system

  values:
    controller:
      type: cronjob
      cronjob:
        schedule: "@hourly"
        concurrencyPolicy: Forbid
    restartPolicy: OnFailure

    image:
      repository: renovate/renovate
      tag: 37.91.4@sha256:0d81278dcaee64aebcba913f8b0b79f5eed4d7b5a3ba067c1787f07f83bce2cc

    env:
      RENOVATE_BASE_DIR: &baseDir "/tmp/renovate"
      RENOVATE_CONFIG_FILE: &configFile "/opt/renovate/config.json"

    envFrom:
      - secretRef:
          name: renovate-secret

    service:
      main:
        enabled: false

    persistence:
      config:
        enabled: true
        name: renovate-config
        readOnly: true
        mountPath: *configFile
        subPath: config.json
        type: configMap
      data:
        enabled: true
        type: emptyDir
        mountPath: *baseDir

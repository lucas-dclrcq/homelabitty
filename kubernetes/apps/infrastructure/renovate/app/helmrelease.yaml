---
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: renovate
  namespace: infrastructure
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: 1.5.1
      interval: 30m
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system

  values:
    controller:
      type: cronjob
      cronjob:
        schedule: "@hourly"
        concurrencyPolicy: Forbid
    restartPolicy: OnFailure

    image:
      repository: renovate/renovate
      tag: 37.128.2@sha256:c5ca3bb1bf43e9efa9dabad710d573e7e3ffaff3a26f294eda3171adfdd9628b

    env:
      RENOVATE_BASE_DIR: &baseDir "/tmp/renovate"
      RENOVATE_CONFIG_FILE: &configFile "/opt/renovate/config.json"

    envFrom:
      - secretRef:
          name: renovate-secret

    service:
      main:
        enabled: false

    persistence:
      config:
        enabled: true
        name: renovate-config
        readOnly: true
        mountPath: *configFile
        subPath: config.json
        type: configMap
      data:
        enabled: true
        type: emptyDir
        mountPath: *baseDir

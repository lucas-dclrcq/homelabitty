---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: redpanda-operator
spec:
  interval: 1h
  chart:
    spec:
      chart: operator
      version: v25.2.1-beta1
      sourceRef:
        kind: HelmRepository
        name: redpanda
  values:
    crds:
      enabled: true

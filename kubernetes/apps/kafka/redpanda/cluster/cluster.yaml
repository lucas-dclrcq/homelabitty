---
apiVersion: cluster.redpanda.com/v1alpha2
kind: Redpanda
metadata:
  name: redpanda
spec:
  clusterSpec:
    statefulset:
      replicas: 3
    storage:
      persistentVolume:
        enabled: true
        size: 20Gi
        storageClass: openebs-hostpath

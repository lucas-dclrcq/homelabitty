---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: apicurio-ui
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: 3.5.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    controllers:
      apicurio:
        containers:
          app:
            image:
              repository: apicurio/apicurio-registry-ui
              tag: 3.0.3

            env:
              REGISTRY_API_URL: "https://apicurio-api.${LIMITED_DOMAIN}/apis/registry/v3"

            resources:
              requests:
                cpu: 200m
                memory: 100Mi
              limits:
                memory: 250Mi

    service:
      app:
        controller: apicurio
        ports:
          http:
            port: 8080

    ingress:
      betanin:
        className: internal
        hosts:
          - host: &host "apicurio.${LIMITED_DOMAIN}"
            paths:
              - path: /
                service:
                  identifier: app
                  port: http
        tls:
          - hosts:
              - *host
            secretName: "${LIMITED_DOMAIN/./-}-production-tls"

---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app sqq-vente-groupee-staging
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: *app
  values:
    controllers:
      main:
        replicas: 2
        containers:
          main:
            image:
              repository: ghcr.io/superquinquin-sur-deule/sqq-achat-groupe
              tag: latest@sha256:f82b5be53e5d4fdaabaf1d93d3bdcf69b3a3cb6e4e5e245f3d72732f8c0fb335
            envFrom:
              - secretRef:
                  name: sqq-vente-groupee-staging-secret
            resources:
              requests:
                cpu: 12m
                memory: 64M
              limits:
                memory: 1Gi
    service:
      main:
        controller: main
        ports:
          http:
            port: &port 8080
    route:
      app:
        hostnames:
          - "sqq-vente-groupee-staging.${SECRET_DOMAIN}"
        parentRefs:
          - name: external
            namespace: network
        rules:
          - backendRefs:
              - name: *app
                port: *port

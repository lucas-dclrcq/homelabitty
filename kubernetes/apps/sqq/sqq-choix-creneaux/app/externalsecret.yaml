---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: sqq-choix-creneaux
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: hashicorp-vault
  target:
    name: sqq-choix-creneaux-secret
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        QUARKUS_DATASOURCE_JDBC_URL: '{{ index . "jdbc-uri" }}'
        QUARKUS_OIDC_CREDENTIALS_SECRET: "{{ .OIDC_CLIENT_SECRET }}"
        BREVO_ENABLED: "true"
        BREVO_API_KEY: "{{ .BREVO_API_KEY }}"
        ODOO_URL: "{{ .ODOO_URL }}"
        ODOO_DB: "{{ .ODOO_DB }}"
        ODOO_USERNAME: "{{ .ODOO_USERNAME }}"
        ODOO_PASSWORD: "{{ .ODOO_PASSWORD }}"
  dataFrom:
    - extract:
        key: sqq/sqq-choix-creneaux
      sourceRef:
        storeRef:
          kind: ClusterSecretStore
          name: hashicorp-vault
    - extract:
        key: postgres-sqq-pguser-sqqchoixcreneaux
      sourceRef:
        storeRef:
          kind: ClusterSecretStore
          name: crunchy-pgo-sqq-secrets

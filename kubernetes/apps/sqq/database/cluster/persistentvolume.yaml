---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-sqq-pgbackrest-repo
  labels:
    app.kubernetes.io/component: sqq-pgbackrest-repo
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  nfs:
    server: "truenas.${INTERNAL_DOMAIN}"
    path: /mnt/tank/Backup/postgresql/sqq

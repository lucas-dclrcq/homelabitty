---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: metadada-meilisearch
spec:
  storageClassName: ceph-block
  resources:
    requests:
      storage: 100Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce

---
endpoints:
  - name: radarr
    group: guarded
    url: 1.1.1.1
    interval: 1m
    dns:
      query-name: "radarr.${SECRET_DOMAIN}"
      query-type: A
    conditions:
      - "len([BODY]) == 0"

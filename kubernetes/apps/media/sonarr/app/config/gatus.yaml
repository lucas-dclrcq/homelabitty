---
endpoints:
  - name: sonarr
    group: guarded
    url: 1.1.1.1
    interval: 1m
    dns:
      query-name: "sonarr.${SECRET_DOMAIN}"
      query-type: A
    conditions:
      - "len([BODY]) == 0"

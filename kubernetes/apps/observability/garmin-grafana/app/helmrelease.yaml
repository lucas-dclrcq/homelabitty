---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app garmin-grafana
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  values:
    controllers:
      main:
        annotations:
          reloader.stakater.com/auto: "true"

        containers:
          main:
            image:
              repository: ghcr.io/arpanghosh8453/garmin-fetch-data
              tag: latest@sha256:73a5120e8210306dea190932a443a50cbcfcb5c0aca0a5cc0a2b0e1c8534837d
            envFrom:
              - secretRef:
                  name: garmin-grafana-secret
            env:
              INFLUXDB_HOST: influxdbv1.observability.svc.cluster.local
              INFLUXDB_PORT: 8086
              INFLUXDB_DATABASE: GarminStats
              UPDATE_INTERVAL_SECONDS: 300
              LOG_LEVEL: INFO

    persistence:
      tokens:
        existingClaim: *app
        globalMounts:
          - path: /home/appuser/.garminconnect

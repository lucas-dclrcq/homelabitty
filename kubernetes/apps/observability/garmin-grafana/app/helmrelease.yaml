---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app garmin-grafana
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  values:
    controllers:
      main:
        annotations:
          reloader.stakater.com/auto: "true"

        containers:
          main:
            image:
              repository: ghcr.io/arpanghosh8453/garmin-fetch-data
              tag: latest@sha256:e313ea8d11e2c71b1ec9e5a9465f144dbf914dc5edc0e1da72ea2f1ee6ea8f3f
            envFrom:
              - secretRef:
                  name: garmin-grafana-secret
            env:
              INFLUXDB_HOST: influxdbv1.observability.svc.cluster.local
              INFLUXDB_PORT: 8086
              INFLUXDB_DATABASE: GarminStats
              UPDATE_INTERVAL_SECONDS: 300
              LOG_LEVEL: INFO

    persistence:
      tokens:
        existingClaim: *app
        globalMounts:
          - path: /home/appuser/.garminconnect
